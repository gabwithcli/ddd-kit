{
    "name": "@acme/monorepo",
    "private": true,
    "packageManager": "pnpm@9.0.0",
    "workspaces": [
        "packages/*",
        "apps/*"
    ],
    "scripts": {
        "build": "pnpm -w -r --filter ./packages... run build"
    },
    "pnpm": {
        "overrides": {
            "zod": "^3.23.0",
            "drizzle-orm": "^0.44.4"
        }
    }
}